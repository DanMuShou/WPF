<Window
  x:Class="ProductionMonitoringPlatform.Views.SettingWindow"
  x:Name="Window"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize"
  Padding="8,4"
  Title="系统配置"
  Height="450"
  Width="800"
>
  <Window.Resources>
    <Style TargetType="RadioButton">
      <Setter Property="Background" Value="Transparent"></Setter>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="RadioButton">
            <Border Background="{TemplateBinding Background}" Height="30">
              <ContentPresenter
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Margin="8,0"
              ></ContentPresenter>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsChecked" Value="True">
          <Setter Property="Background" Value="LightSkyBlue"></Setter>
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="30"></RowDefinition>
      <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="130"></ColumnDefinition>
      <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <TextBlock
      Grid.Row="0"
      Grid.Column="0"
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
      Text="配置信息"
    ></TextBlock>
    <Border Grid.Row="1" Grid.Column="0" BorderThickness="0,0,1,0" BorderBrush="#ddd"></Border>
    <StackPanel Grid.Row="1" Grid.Column="0">
      <RadioButton
        Content="设备清单"
        IsChecked="True"
        Tag="L1"
        Command="{Binding ElementName=Window, Path=JumpToTitleCommand}"
        CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
      ></RadioButton>
      <RadioButton
        Content="环境检测"
        Tag="L2"
        Command="{Binding ElementName=Window, Path=JumpToTitleCommand}"
        CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
      ></RadioButton>
      <RadioButton
        Content="设备监测"
        Tag="L3"
        Command="{Binding ElementName=Window, Path=JumpToTitleCommand}"
        CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
      ></RadioButton>
      <RadioButton
        Content="配置一"
        Tag="L4"
        Command="{Binding ElementName=Window, Path=JumpToTitleCommand}"
        CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
      ></RadioButton>
      <RadioButton
        Content="配置二"
        Tag="L5"
        Command="{Binding ElementName=Window, Path=JumpToTitleCommand}"
        CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
      ></RadioButton>
    </StackPanel>
    <ScrollViewer
      Grid.Row="1"
      Grid.Column="1"
      HorizontalScrollBarVisibility="Disabled"
      VerticalScrollBarVisibility="Hidden"
    >
      <Frame
        x:Name="InfoFrame"
        NavigationUIVisibility="Hidden"
        HorizontalAlignment="Stretch"
        Source="SettingPage.xaml"
      ></Frame>
    </ScrollViewer>
  </Grid>
</Window>
