<UserControl
  x:Class="ConsumptionRecord.WPF.Views.PersonalUc"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:prism="http://prismlibrary.com/"
  xmlns:uic="clr-namespace:MaterialDesignColors;assembly=MaterialDesignColors"
  xmlns:vm="clr-namespace:ConsumptionRecord.WPF.ViewModels"
  prism:ViewModelLocator.AutoWireViewModel="True"
  d:DataContext="{d:DesignInstance Type=vm:PersonalUcViewModel}"
  mc:Ignorable="d"
  d:DesignHeight="300"
  d:DesignWidth="300"
>
  <Grid Margin="16,8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0" Orientation="Horizontal">
      <TextBlock Text="浅色" VerticalAlignment="Center"></TextBlock>
      <ToggleButton Margin="8,0" IsChecked="{Binding IsDarkTheme }"></ToggleButton>
      <TextBlock Text="深色" VerticalAlignment="Center"></TextBlock>
    </StackPanel>
    <TextBlock
      Grid.Row="1"
      Text="未完待续"
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
    ></TextBlock>
  </Grid>
</UserControl>
