<UserControl
  x:Class="ConsumptionRecord.WPF.Views.Dialogs.AddWaitUc"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:prism="http://prismlibrary.com/"
  xmlns:ui="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:vm="clr-namespace:ConsumptionRecord.WPF.ViewModels.Dialogs"
  xmlns:md="clr-namespace:ConsumptionRecord.WPF.Models"
  prism:ViewModelLocator.AutoWireViewModel="True"
  d:DataContext="{d:DesignInstance Type=vm:AddWaitUcViewModel}"
  mc:Ignorable="d"
  d:DesignHeight="Auto"
  d:DesignWidth="Auto"
>
  <Grid Margin="16,32,16,24" Width="450" Height="300">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
    </Grid.RowDefinitions>
    <TextBlock FontSize="20" FontWeight="SemiBold" Grid.Row="0" Text="添加待办事项"></TextBlock>
    <StackPanel Margin="0,24,0,0" Grid.Row="1" Orientation="Horizontal">
      <TextBlock VerticalAlignment="Center" Text="状态: "></TextBlock>
      <ComboBox Margin="8,0" SelectedIndex="{Binding SelectedIndex}">
        <ComboBoxItem Content="待办" />
        <ComboBoxItem Content="已完成" />
      </ComboBox>
    </StackPanel>
    <TextBox
      Margin="0,16"
      Grid.Row="2"
      ui:HintAssist.Hint="输入标题"
      Text="{Binding WaitInfo.Title}"
    ></TextBox>
    <TextBox
      Grid.Row="3"
      VerticalContentAlignment="Top"
      TextWrapping="Wrap"
      ui:HintAssist.Hint="输入内容"
      Text="{Binding WaitInfo.Content}"
    ></TextBox>
    <Grid Margin="0,24,0,0" Grid.Row="4">
      <Grid.ColumnDefinitions>
        <ColumnDefinition></ColumnDefinition>
        <ColumnDefinition></ColumnDefinition>
      </Grid.ColumnDefinitions>
      <Button
        Grid.Column="0"
        Margin="4,0"
        Style="{StaticResource MaterialDesignOutlinedButton}"
        Command="{Binding CancelCommand }"
        Content="取消"
      />
      <Button Grid.Column="1" Margin="4,0" Content="保存" Command="{Binding SaveCommand}" />
    </Grid>
  </Grid>
</UserControl>
